# v17 Notes

- Removes the pale/white organic hero shape left over from the earlier light theme.
- Increases still-art background blur to ~4px to better hide upscaling/compression artifacts while preserving recognizability.
- Keeps video blur light (~1.4px) because motion naturally masks compression.
- Restores a sharp rotating homepage artwork spotlight using three randomly selected published works on every page load.
- Lead artwork uses a structured grid with no white mats, rotations, or scrapbook-style decoration.
- Background section artwork is chosen from later items in the shuffled pool when possible to reduce repetition with the lead artwork.
